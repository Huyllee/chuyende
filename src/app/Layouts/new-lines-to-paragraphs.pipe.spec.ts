import { NewLinesToParagraphsPipe } from './new-lines-to-paragraphs.pipe';

describe('NewLinesToParagraphsPipe', () => {
  it('create an instance', () => {
    const pipe = new NewLinesToParagraphsPipe();
    expect(pipe).toBeTruthy();
  });
});
